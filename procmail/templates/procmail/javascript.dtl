<script>
    function reverse(view_name, args, kwargs){
        return $.ajax(
            "{% url "procmail:reverse" %}",
            {   
                method: "POST",
                data: {view_name:view_name, args:JSON.stringify(args), kwargs:JSON.stringify(kwargs)}
            }
        );
    }
    var curr_id = "{{curr_stmt.id}}";
  </script>
